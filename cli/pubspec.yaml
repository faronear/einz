name: einz_cli
description: Einz CLI 测试端（无 UI，Phase 0-4 测试驱动）
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  einz_shared:
    path: ../shared
  args: ^2.5.0
  crypto: ^3.0.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.32.0
