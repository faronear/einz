name: einz_shared
description: Einz 客户端共享核心（crypto/protocol/sync）——纯 Dart，App 与 CLI 共用
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  crypto: ^3.0.0
  sodium: ^2.3.1 # ≥2.3.1：pwhash 为 crypto.pwhash 子对象（str/call），旧版直挂方法已移除
  uuid: ^4.3.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
