# OnlySpace App（Flutter）

V1 手机客户端（iOS / Android）。待安装 Flutter SDK 后初始化工程骨架。

- 框架：Flutter + Dart
- 加密：sodium_libs（dart:ffi）
- 安全存储：flutter_secure_storage
- 本地库：drift（SQLite）
- 依赖 `shared/` 核心包

详见 `aimemo/projectPlan.md` Phase 3。
