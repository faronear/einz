/// 弹出菜单的统一尺寸——老板 2026-09-22：菜单太长，压缩行距、特别是分割线上下的留白。
///
/// Flutter 的默认值：行长 `kMinInteractiveDimension`=48、分割线 `_kMenuDividerHeight`=16、
/// 菜单整体上下各 8。菜单项一多（聊天页 13 项 + 4 条分割线）整列就显得松散。
/// 这里统一收窄，三个菜单（聊天页 / 锁屏页 / 向导页）共用，避免各自漂移。
///
/// **代价**：行长 40 低于 Material 规定的 48 触控最小目标——桌面端无碍，手机上点击热区
/// 略小，但仍在可点范围。若日后觉得手机端太挤，改回 44 即可（只改这一处）。
const double kMenuRowHeight = 40;

/// 分割线占位高度（线本身 1px 居中）：16 → 8，上下各留 ~3.5px。
const double kMenuDividerHeight = 8;
