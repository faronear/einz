# OnlySpace 部署环境变量模板（deployment/.env）
#
# 用法：复制本文件为 deployment/.env 并填入真实值。
#   cp deployment/.env.example deployment/.env
# .env 已在 .gitignore 中，不入库、git pull 不覆盖。
#
# 注意：变量名在 2026-08 已由 ONLYSPACE_BACKUP_KEY 改名（命名消歧）——
# 若旧部署 .env 里还是旧名，请手动改名，否则 npm run backup 会拒绝执行。

# 服务端备份/恢复归档密钥（npm run backup 用，AES-256-GCM）
# 生成：python3 -c "import os,base64;print(base64.b64encode(os.urandom(32)).decode())"
ONLYSPACE_DB_BACKUP_KEY=粘贴32字节base64密钥
